# /etc/caddy/caddy.env — read by the caddy service through a systemd drop-in
# (deploy/SETUP.md §7). Replace the example values.
SITE_DOMAIN=www.example.com
ACME_EMAIL=it@example.com
# Backend phase, once object storage serves public files:
# ASSETS_DOMAIN=assets.example.com
